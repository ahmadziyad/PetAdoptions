{
  "name": "adoptions-app",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Java Spring Boot app - no build needed for static deployment'",
    "vercel-build": "echo 'Build completed'"
  },
  "engines": {
    "node": ">=18"
  }
}